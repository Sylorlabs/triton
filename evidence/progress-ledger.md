# Master Plan Progress Ledger

Environment snapshot (2026-07-07): Triton starting commit `1e83207`; Zag
starting commit `807c03d`; compiler SHA-256
`713f3b60dfa07a6e8f2683a06de854ec40de495469a4b9cf0f3a03c40ba5eed6`;
Linux `7.0.11-76070011-generic`; x86-64; X11 `:0`; PowerColor Radeon RX 5700 XT
is the only GPU and drives the display.

| Item | State | Change / evidence | Last result | Next |
|---|---|---|---|---|
| A.inventory | pass | `rg --files`; source/probe/tool inventory recorded during baseline | 69 Zag/Python modules plus scripts/docs identified | Keep updated in evidence index |
| A.compiler | pass | `../zag/zag-poc/znc`, sibling Git commit `807c03d`, SHA above | Supported self-hosted compiler identified | Re-record at release |
| A.single-test-entry | pass | `verify.sh`, `tools/verify.zag` | Pure-Zag orchestrator emits JSON result records | Run safe gate |
| A.baseline-engine | pass | `./verify.sh safe`; `probe/engine_test.zag` | 61 assertions passed after replacing a stale user-specific temp path | Expand persistence fault coverage |
| A.gpu-default-safety | pass | `build.sh`, `tools/verify.zag` | GPU memory and dispatch probes are explicit modes; default does not touch DRM | Keep GPU opt-in |
| B.versioned-model | pass | `src/device_model.zag`; `probe/engine_test.zag` | Schema v1 carries units, source, method, date, uncertainty, confidence, and known state | Expand per-component bindings |
| B.derived-timing | pass | `changing device model changes derived timing without source edit` | Runtime model-rate change altered compiled symbol clock | Add dimensional property coverage |
| B.incomplete-model | pass | `incomplete model cannot silently advance verified simulation` | Missing group index sets `model_ok=false` and blocks stepping | Expose complete diagnostics in agent/UI |
| B.model-roundtrip | pass | `roundtrip: device model version pinned`; `physical model values preserved` | Project format v2 preserved schema and physical inputs | Add corruption/migration fixtures |
| B.model-version-diagnostic | pass | `agent_check_device_model_version`; `device-model-version` | Agent open rejects incompatible device-model schema with stable `E_MODEL_VERSION ... expected 1` diagnostic | Extend to future schema migration hints |
| B.claim-cleanup | pass | `README.md`; `tools/verify.zag` unsupported-claim audit | README separates illustrative, simulated, measured, and unknown evidence | Extend audit allowlist and generated evidence |
| E.native-automation | pass | native `--agent`, `--mcp`, `zagctl`; legacy Python client removed | Supported automation path is compiled Zag | Add capability enforcement |
| E.flash-fir-import | pass | `flash import`; `src/flash_ir.zag`; `./verify.sh safe` | Flash FIR v1 maps to production components/routes without executing host code | Add UI import dialog and trace panel |
| C.massive-flash-pcu | pass | `../flash/examples/photonic_massive.flash`; `flash-photonic` gate | 64 ops became 384 components including 64 memory tiles and 192 routed guides; zero detector mismatches | Extend all-vector oracle coverage |
| C.reference-integrity | pass | `scene_validation_errors`; `reference-tamper` | Canonical PCU has zero structural errors; geometry, model, and connectivity mutations each fail verification | Add timing-margin report |
| D.flash-ternary-ops | pass | chamber lattice ops 2/3/5/7; constant-trit emitters | Flash neg/add/mul/nand semantics and exhaustive truth tables pass | Add larger composed-network oracle |
| D.simulation-modes | pass | `SimMode.Functional`, `SimMode.Physical`; `simulation-properties` | Functional behavior is independent of missing physical data; physical mode rejects it | Add deterministic trace capture |
| D.property-oracle | pass | `probe/simulation_property_test.zag`; `simulation-properties` | Exhaustive Flash truth tables, explicit invalid states, delay linearity/rate monotonicity all pass | Add graph differential/metamorphic suites |
| D.deterministic-trace | pass | `trace`; `examples/flash_photonic_massive.trace.txt`; `deterministic-trace` | Two fresh 32-symbol massive-PCU traces match each other and the golden bytes | Add trace viewer and breakpoints |
| D.incremental-differential | pass | `simulation-properties`; independent save/reload/full compile | Every node history and guide ring matches incremental recomputation before/after 24 further symbols | Add translation/equivalent-route metamorphic cases |
| D.metamorphic | pass | translated networks, alternate shortest route, save/reload in `simulation-properties` | Complete simulator state remains identical across all three transformations | Add randomized transformation seeds |
| G.flash-roundtrip | pass | `examples/flash_photonic_massive.zpa`, save/reopen verify | Canonical project matches a fresh public-interface reconstruction byte-for-byte | Keep comparison in the safe gate |
| G.deterministic-exports | pass | `examples/flash_photonic_massive.{bom,netlist,models,report}.txt`; `flash-photonic` | Regenerated project and all four exports compare byte-for-byte with maintained artifacts | Add render pixel-diff gate |
| G.atomic-save | pass | `atomic_write_file`; engine failure injection | temp write, file fsync, close, rename; failed rename retained destination and removed temp | Add parent-directory fsync and crash-stage matrix |
| G.corruption | pass | project v2 checksum; `corrupt project checksum rejected` | Modified project byte rejected before scene mutation | Add recovery journal UX |
| G.project-version-roundtrip | pass | `engine`; `zpa 1` legacy fixture; `zpa 2` save/load | Current v2 round-trips deterministically; legacy v1 loads, migrates to v2, reloads, and preserves IDs | Add future-version migration fixtures when schemas change |
| E.capability-default | pass | `src/capability.zag`; `agent-capability-denial` | default read/inspect/simulate; unauthorized placement leaves project hash unchanged | Add scoped/expiring grants |
| E.scoped-grants | pass | `TRITON_GRANT`; `agent-scoped-grants` | Optional grant narrowing enforces project, session, operation class, path prefix, and monotonic expiration while preserving coarse `TRITON_CAPS` | Add persisted user-facing grant editor |
| E.user-rules | pass | `TRITON_ALLOW_OPS`; `TRITON_DENY_OPS`; `agent-user-rules` | User allow rules can grant an operation class, deny rules override full caps without mutation, and hard path confinement still rejects outside-root writes | Add persisted UI rule management |
| E.path-confinement | pass | `TRITON_PROJECT_ROOT`; `agent-path-confinement` | Outside-root and lexical parent traversal writes rejected with stable error; no file created | Add symlink-safe descriptor-relative access |
| E.revision-precondition | pass | `TRITON_EXPECT_REV`; `agent-revision-conflict` | Stale client leaves project bytes/revision unchanged; refresh then succeeds | Add atomic cross-process commit lock |
| E.idempotency | pass | mandatory CLI `request`; MCP `triton_mutate`; persistent `.idem` | Unkeyed mutation rejected; first result carries transaction metadata; replay is byte/revision inert | Add atomic cross-process key reservation |
| E.protocol-negotiation | pass | native `capabilities`; MCP initialize; `agent-negotiation`, `mcp-protocol` | Grants, revision, limits, Triton protocol, and MCP version are machine-readable; framing is bounded | Add schema coverage for operations not yet first-class |
| E.mcp-specialized-mutations | pass | generated MCP tool schemas; `mcp-specialized-mutations`, `mcp-tool-coverage` | Specialized mutation tools advertise `idempotency_key`/`expected_revision`; MCP covers place/get/delete/can_place/export/trace/Flash import/verify through shared agent dispatch | Add remaining create/close/move/rotate/disconnect/reroute schemas |
| E.tool-invariants | pass | `mcp-invariant-enforcement` | Invalid MCP placement/routing cannot bypass design rules and leave project bytes/revision unchanged | Extend to every future specialized tool |
| E.audit-results | pass | `agent_mutation_ok`; append-only audit; `agent-audit-result` | Actor, validated request, result, revision, affected ID, and undo token verified | Add audit rotation/retention policy |
| E.audit-redaction | pass | `audit_redact`; `audit-redaction` | Actor/request/result audit records scrub secret/password/XAuthority/cookie-looking tokens, preserve required undo-token metadata, and do not emit opened project content | Add configurable redaction patterns and retention policy |
| E.undo-audit | pass | `agent-undo-audit` | Public-agent place, route, delete, undo, and redo mutations change state as expected and audit affected IDs plus undo tokens | Extend to future move/rotate/reroute verbs |
| E.error-contract | pass | normalized `ERR E_* diagnostic`; `agent-error-codes` | Generic and specific machine codes retain human-readable context through CLI/MCP | Publish complete error-code reference |
| K.resource-limits | pass | `src/limits.zag`; `bounded-inputs` | Project/components/guides/path depth, route search, trace history, simulation request, images, FIR/scripts, and agent requests bounded | Keep limits under workload review |
| K.transactional-load | pass | temporary-scene parse/swap; engine hostile fixtures | Invalid kind/coordinate loads fail and preserve the active scene | Add hostile guide and randomized parser corpus |
| K.content-nonexecution | pass | literal shell-shaped project name; `security-regression` | Load/save/export preserve text as data and never create command sentinel | Extend stable hostile corpus |
| K.parser-fuzz | pass | checked-in project/FIR/journal corpus; 32 deterministic byte mutations; MCP boundary corpus | Malformed inputs reject transactionally; accepted mutations reserialize/reload; integer overflow rejected | Grow corpus from future failures |
| K.diagnostics-privacy | pass | `diagnostics`; `diagnostics-privacy` | Diagnostic bundle writes metadata only, marks scene contents excluded by default, and omits private sentinels/raw project records | Add selectable redacted scene summaries |
| L.formats-and-recovery-docs | pass | `README.md`; `docs/FORMATS_AUTOMATION_RECOVERY.md` | Supported workflow, format, units/provenance, agent/MCP, persistence, and limitations documented without overclaiming | Keep synchronized through release gate |
| L.reference-pcu-reproduction | pass | `docs/REPRODUCE_REFERENCE_PCU.md`; `flash-photonic`; `deterministic-trace` | Public agent import, Flash rebuild, canonical project/export comparisons, and trace verification have reproducible commands | Add screenshot/UI reproduction once visible controls exist |
| L.language-audit | pass | `unsupported-claim-audit`; `stale-language-audit`; `src/gpu_rt.zag` comment cleanup | Positive marketing/probe-era phrases and unsupported hardware/performance claims are rejected by the safe gate | Keep exception list narrow and reviewed |
| L.release-notes | pass | `docs/RELEASE_NOTES.md`; safe-gate suite names | Release notes enumerate verified behavior and explicit non-claims without using commit count as evidence | Regenerate or review before release tags |
| R.final-safe-subset | pass | `build-production`; `x11-live`; `x11-captures`; `flash-photonic`; `reference-tamper`; `deterministic-trace` | Final-gate build, X11 launch, agent PCU construction, verification, and output/trace comparison are proven by the safe gate | UI manual workflow, crash recovery, CPU soak, and GPU-safe gates remain unchecked |
| M.checked-evidence | pass | `probe/masterplan_evidence_test.zag`; `masterplan-evidence` | Every checked checkbox in `masterplan.md` has an `Evidence:` reference before the next checkbox | Keep this gate required before checking future items |
| E.audit-log | partial | append-only request/denial records with actor and required capability | command results/affected IDs/undo tokens not yet recorded | Complete mutation result records |
| F.x11-live-baseline | pass | `./verify.sh safe`; `--x11-selftest` on `DISPLAY=:0` | Motion, modifier, key, button, WM_DELETE, and PutImage round trip passed | Add resize/focus/reopen coverage |
| F.x11-interleaving | pass | `X11.synth_seen`; live selftest | Synthetic proof no longer flakes when real pointer events interleave | Add resize/focus synthetic cases |
| F.x11-resize-focus-reopen | pass | `x11-live` | Real ConfigureWindow/ConfigureNotify, FocusIn, close, and reopen passed | Add long UI soak |
| F.x11-multisize-capture | pass | `x11-captures`; 1024x640 and 1440x900 | Both live X11 windows presented and emitted deterministic CPU captures | Archive reference-PCU captures |
| F.layout-persistence | pass | `ui-preferences` | left/right/bottom splits round-trip separately from project data | Add invalid preference fuzz cases |
| F.ui-tokens | pass | `src/ui.zag`; `ui-tokens` | Spacing, typography, shape/elevation, UI state colors, separate beam colors, and text/panel contrast are defined and gated | Add full WCAG-style contrast ratios and usage coverage |
| F.frame-overlay | pass | `draw_frame_timing_overlay`; `frame-overlay` | Viewport renders frame number, previous frame time, simulation symbol, and run state in a tested overlay | Add rolling averages and subsystem timing split |
| H.frame-diff | pass | `probe/frame_diff_test.zag`; `frame-diff` | Fixed-state CPU frame captures are byte-identical and the pixel-diff helper detects a deliberate one-pixel change | Add maintained golden scene image corpus |
| H.fb-bounds | pass | `probe/fb_bounds_test.zag`; `fb-bounds` | Clipped framebuffer point/fill/blend/line/text operations preserve canary pixels outside active clip | Add randomized primitive clipping corpus |
| I.render-discovery | pass | `gpu_open`; `--gpu-info` | Scans DRM render minors and accepts only a successful AMDGPU info query; found minor 128 | Query full firmware/IP/memory tuple |
| I.display-gpu-certification | blocked | `lspci`, `/dev/dri`; masterplan 12.1 | Only RX 5700 XT is also the display GPU; destructive reset/fault certification requires a non-display GPU | Complete all bounded non-destructive work; do not fake certification |
| M.optimizer-core | pass | `src/optimizer.zag`; `probe/optimizer_test.zag`; `optimizer` gate | Photon Solver core: named cost model, per-symbol detector output-equivalence over a 64-symbol horizon, dead-path rewrite family, strict-observer analyze (byte-identical project before/after), apply with re-verification; 15/15 assertions pass and full safe gate stays `failures:0` | Add background scheduler, UI Optimizer button/panel + Settings, undo/audit-routed apply, and further rewrite families (CSE, route consolidation, algebraic identities) |
| M.optimizer-mem | pass | `scene_free`, `scene_clone` (scene.zag); `sim_free` (sim.zag) | Reusable teardown/clone primitives so the always-on optimizer clones and simulates without leaking; production binary still builds | Reuse buffers across passes once the background scheduler lands |
| M.optimizer-engine | pass | `OptEngine` in `src/optimizer.zag`; `optimizer` gate (27/27) | Background engine: enabled/auto_apply(off by default)/min_gain settings, bounded `opt_engine_tick`, silent badge count, Apply/Ignore/Details actions, applied-count; proven pause/disable=only-speed, auto-apply commits, gain threshold suppresses | Add explicit time/memory budget + watchdog; build the Optimizer button + Settings UI |
| M.optimizer-family2 | pass | `opt_find_const_collapse`, generalized removals+relabels proposal model; `optimizer` gate | Second rewrite family (Sonnet-scouted, reviewed): constant-op collapse folds mul-by-(-1)/min-with-(+1) chambers to a negate and drops the constant emitter; unified `opt_build_candidate`/`opt_edits_win`/`opt_apply` verify every family via output-equivalence replay | Implement duplicate-chamber CSE (family 1 from scout report) with guide-repointing; then route-consolidation |
| M.optimizer-app | pass | `App.opt`, throttled `opt_engine_tick` in `app_frame`, status-bar badge (workspace.zag); `--smoke`, `x11-live`, `x11-captures` | Optimizer wired into the live app as a runtime observer: throttled (~1/s), idle-gated, off the interaction path; auto-apply off so it never mutates the project at runtime; quiet count-only status badge; production binary builds and X11 launch/capture stay green | Add interactive Optimizer button + panel (Apply/Ignore/Details) and Settings button/gear in the live-UI phase |
| M.optimizer-ui | pass | menu-bar `opt` button + gear (`draw_menubar`, `draw_gear`), `draw_opt_panel`, `draw_settings_panel`; `probe/opt_ui_test.zag`; `optimizer-ui` gate (11/11) | Interactive UI wired to the engine and proven by driving the real `app_frame` with injected clicks: Optimizer button+badge opens a panel with family/gain summary and Apply/Ignore/Details; Settings gear opens a real panel with Optimizer/Auto-apply/Min-gain; Apply removes the part and marks dirty; outside-click closes. Renders under live X11 | Add ignore-memory, make buttons keyboard-accessible, persist settings to layout.cfg |
| F.command-palette | pass | `PCmd`/`palette_commands`/`draw_palette`/`app_palette_keys` (workspace.zag), Ctrl+K in `app_keys`; `probe/palette_test.zag`; `palette` gate (9/9) | Command palette (Ctrl+K/Ctrl+P): fuzzy substring filter over file/edit/view/tool/place/optimizer/settings commands, arrow+enter and click execution, Esc/outside-click close; proven by driving the real frame loop with injected keys and clicks | Add jump-to-object entries and recent/most-used ordering |
| F.shortcuts | pass | `app_keys` (app.zag); `probe/shortcuts_test.zag`; `shortcuts` gate (9/9) | Core editor shortcuts verified end-to-end: Q select, E route, G move, R rotate, Shift+D duplicate, X/Del delete, F frame-selected, Space run/pause | Add copy/paste shortcuts |
| D.exec-bounds | pass | `probe/bounds_test.zag`; `exec-bounds` gate (5/5) | sim_update caps steps/call at 4096 under a huge dt; guide delay-line depth clamps for an absurd length; trace-history ring fixed; simulator consistent after the burst | Add a hostile-graph fan-out bound test |
| D.robustness | pass | `probe/robustness_test.zag`; `robustness` gate (7/7) | Physical mode detects a model gap (group_index unknown) and blocks stepping; invalid ternary inputs propagate as error; every node output stays a bounded valid trit over 64 symbols (no overflow) | Add explicit overflow injection on the complex-field accumulator |
| E.agent-ops | pass | move/rotate/disconnect/reroute/inspect/close in `agent.zag`, `scene_set_rot` + `op_rotate` (kind-7 journal); `agent-ops` gate | Full agent operation set through one command layer with envelope/capability enforcement; move + rotate undoable (kind-7 op), reroute re-paths, inspect summarizes model | Add per-op MCP schemas for the new verbs |
| G.persistence-faults | pass | `probe/persistence_test.zag`; `persistence` gate (6/6) | Truncated and bit-corrupted projects rejected on load (checksum), active scene intact; valid project still loads; atomic temp+rename (engine gate) protects the target | Simulate disk-full via a write-fail hook |
| F.render-invalidation | pass | `render_rev`/`scene_mark_render`, `Comp.visible` culling; `probe/render_test.zag`; `render-invalidation` gate (5/5) | Hidden parts culled from picking/render; UI-only frames do not invalidate the scene render cache; scene edits bump render_rev + topo_dirty (independent channels) | Add per-region dirty rects for partial tile updates |
| F.camera | pass | `app_viewport_input` orbit/pan/zoom (app.zag); `probe/camera_test.zag`; `camera` gate (5/5) | MMB/RMB orbit, Shift+MMB pan, wheel zoom, frame-all bounded distance, all driven through the real input path | Add momentum/easing and configurable sensitivity |
| C.routing | pass | `route_guide` A* (routing.zag); `probe/routing_test.zag`; `routing` gate (4/4) | 3D layer-band routing (y 1..12, 6-connected), named cost terms (step/bend/layer/margin), deterministic paths + replayable failures | Add named-cost inspector UI and manual-route preservation on reroute |
| C.design-rules | pass | `scene_can_place_ext` diagnostic codes, `scene_place` enforcement, `flash_route`/`route_guide`, `scene_port_in_use`; `probe/drc_test.zag`; `design-rules` gate (12/12) | Placement DRC enforced with distinct codes (occupancy=1, ground-only=2, needs-plate=3, bounds=6); invalid-endpoint routes rejected; port-use tracked for fan-in; structural validity via scene_validation_errors (also in reference-tamper) | Add explicit minimum-bend-radius, clearance, and layer-transition rule tests |
| D.stepping-breakpoint | pass | `sim_set_breakpoint`/`sim_clear_breakpoint` + `sim_update` check (sim.zag); `probe/stepping_test.zag`; `stepping` gate (11/11) | Reproducible stepping (step->reset->step identical), reset re-initializes symbol/state, pause flag gates `sim_update`, and a component-trit breakpoint pauses exactly when hit and resumes when cleared. Breakpoint defaults off — existing sims unchanged | Add multi-condition and edge-triggered breakpoints; expose in the UI transport |
| D.deterministic-schedule | pass | `probe/scheduling_test.zag`; `scheduling` gate (3/3) | Same network built in two placement orders (different comp ids + node-list order) yields identical detector output over 48 symbols; sim_step iterates insertion-ordered lists (deliver-all -> compute-all -> launch-all), never hash-map order | Extend to a larger multi-chamber network with feedback |
| K.lifecycle-leak | pass | `probe/lifecycle_test.zag`; `lifecycle` gate (3/3) | 1500 edit/open/close cycles (build+route, simulate, save, reopen, free via scene_free/sim_free); max RSS ~12 MB confirms no unbounded leak, no double-free/use-after-free crash, every reopen valid, allocator healthy afterward | Add a handle-count assertion once the runtime exposes allocation counters |
| B.load-validation | pass | `model_units_dims_valid`/`units_eq`/`param_dim_ok` (device_model.zag), enforced in `design_load_into`; `probe/units_test.zag`; `units-dims` gate (8/8) | Load-time unit+dimension validation: rejects wrong unit strings and out-of-range known values (group index <1, non-optical wavelengths, non-positive rates), unknown params allowed (not faked). Transactional — rejected load preserves the active scene; valid projects (reference PCU) unaffected | Add unit-conversion coverage for every derived quantity |
| B.provenance-display | pass | `kind_model_param` (components.zag), `draw_provenance`/`fmt_f1` (workspace.zag); `probe/provenance_test.zag`; `provenance` gate (10/10) | Inspector shows full physical-model provenance for the selected part: value/units, confidence class + uncertainty, source · date (kind→parameter mapped); guides show group-index provenance; unknown params render "Unknown / Not characterized", never faked. 110 GHz stays model-derived + labeled (unsupported-claim audit green) | Add per-parameter uncertainty ranges and inline provenance for user-entered overrides |
| F.soak | pass | `probe/soak_test.zag`; `soak` gate (4/4) | 3000-frame interactive soak with the sim playing while placing/copy-paste/delete/undo/redo/camera edits churn; asserts no crash, sustained scene validity, bounded growth, bounded frame time. Captured distribution: avg 2ms, max ~10ms, 0 frames over the 16ms 60fps budget (headless x86-64) | Extend to a multi-hour soak and per-hardware frame-time tuples once GPU certified |
| F.hidpi | pass | `fb_upscale` (fb.zag), `App.ui_scale` + Settings/palette control, logical input remap + upscaled present (`run_x11`, main.zag); `probe/dpi_test.zag`; `dpi` gate (7/7) | HiDPI via logical-resolution rendering upscaled to physical pixels — the entire UI scales uniformly with zero UI-code changes. Pixel-exact nearest-neighbor upscale (2x/3x), safe on undersized dst, scale setting cycles 1x/2x/3x; 2x capture verified. Live 1x path byte-unchanged (x11-live/x11-captures green) | Wire ui_scale into the pipe loop; persist scale to layout.cfg; auto-detect display DPI |
| F.outlines | pass | `hover_comp` (app.zag), `line3z`/`vp_box_wire_ghost` (viewport.zag), selection+hover render (workspace.zag); `probe/outline_test.zag`; `outline` gate (4/4) | Selection = dim ghost silhouette through occluding geometry (depth-test-bypass `always` line path) + bright visible outline; subtle hover outline for the unselected hovered part. Hover tracking proven via the real input path; determinism preserved (frame-diff) | Add fade-in animation and thickness scaling by DPI |
| F.volumetric-guides | pass | `draw_guide` (viewport.zag); `--smoke`, `frame-diff`, `flash-photonic`, `x11-captures` | Waveguides render as true volumetric tubes: each 6-connected path segment is an axis-aligned box (square cross-section) with a rim-lit edge, overlapping at turns so corners join; live trit color preserved. Reference PCU (192 guides) still verifies deterministically; smoke frame_ms=4 | Add round/beveled joints and per-beam glow for active carriers |
| F.viewport-polish | pass | `vp_grid`/`lerp_color` (viewport.zag); `--smoke`, `frame-diff`, `x11-captures` | Grid lines fade toward the edges (distance recession) and the world origin gets X/Y/Z axes plus a white origin cube; rendering stays deterministic (frame-diff green) | Add adaptive grid density and a ground-plane shadow |
| F.gizmo | pass | `gizmo_*` (app.zag) + `draw_gizmo`/`fill_disc` (workspace.zag); `probe/gizmo_test.zag`; `gizmo` gate (6/6) | Our own clickable 6-way orientation gizmo: axis balls placed by the live camera basis (rotates with the view), bright labeled positive balls + dim hollow negatives, hover highlight, front-most hit priority; clicking snaps the camera to that axis and consumes the click (no stray pick). Replaces the old static tripod | Add roll indicator and a center home-to-iso ball |
| F.inspector | pass | inspector size + live-output additions (`draw_toolbar`), `sel_output_trit` (app.zag); `probe/inspector_test.zag`; `inspector` gate (5/5) | Inspector now shows exact dimensions (size WxHxD vox) and a live output beam-state swatch/glyph for the selected source; `sel_output_trit` reads the sim history and returns unknown for passive parts | Add port-state and per-component simulation-status readouts, numeric step fields |
| F.picking-edges | pass | `probe/picking_test.zag`; `picking` gate (6/6) | 3D pick correctness verified at occlusion boundaries (nearer AABB wins), viewport corners/boundary pixel (safe, empty), and high zoom in both perspective and orthographic modes | Add port-pick and guide-pick edge cases |
| F.copy-paste | pass | `App.clipboard`, `app_copy`/`app_paste` (Ctrl+C/V, Edit menu, palette codes 15/16); `probe/copypaste_test.zag`; `copypaste` gate (8/8) | Copy snapshots the selection; paste places journaled property-preserving copies at a marching offset, selects them, single-undo/redo, repeatable | Support cross-project paste and paste-at-cursor |
| F.ortho-camera | pass | `Cam.ortho` (math3d.zag), `vp_screen`/`ray_from_pixel` ortho paths, toggle code 26 (menu+palette); `probe/ortho_test.zag`; `ortho` gate (5/5) | Orthographic/perspective camera toggle: parallel projection is depth-independent and stays consistent between rendering, box-select projection, and click picking; proven by projecting an off-plane point in both modes and round-tripping ortho pick | Add pan/zoom/orbit smoothness metrics and a clickable orientation gizmo |
| F.box-select | pass | `app_box_select`/`sel_add` + rubber-band draw (`draw_viewport`), drag wiring in `app_viewport_input`; `probe/boxselect_test.zag`; `boxselect` gate (7/7) | Rubber-band box selection over projected component centers with additive/replace modes, plus shift-toggle multi-select; verified against the real projection | Add object grouping and copy/paste |
| M.optimizer-undo-audit | pass | UOp kind 6 + `op_optimize` (editops.zag), `opt_apply_gui`+`opt_audit` (workspace.zag), `opt_verify`/`opt_engine_refresh` (optimizer.zag); `optimizer` (30/30) + `optimizer-ui` (11/11) gates | Optimizer Apply now routes through the journal + audit like any edit: one batch undo op reverts every removal+relabel (proven undo+redo, GUI and unit), and each apply writes an `state=optimize family=... gain_pct=...` audit record. Both manual Apply and auto-apply share `opt_apply_gui` (re-verifies before commit). `.jrn` persistence stays safe (kind-6 → empty journal, in-session undo only) | Add capability-grant gating + idempotency/revision fields for parity with the agent request path; in-UI optimization history view |

Unchecked items in `masterplan.md` remain `not-run` unless this ledger or a
linked dated report gives a more specific state.
